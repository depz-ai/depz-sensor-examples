# DEPZ sensor SDK: HC-SR04, VL53L8CX/CH, BNO086.
# Pulls in pyserial and numpy.
depz-sensor-sdk>=0.1.3

# Example project windows: the time plot for the ultrasonic example projects, the 8x8 image for ToF.
# OpenCV rather than matplotlib: a sensor gives you a stream of frames, not a
# graph — imshow plus waitKey is the normal way to show one, while matplotlib
# needs blitting and callbacks to keep up. The terminal-only modes run without it.
opencv-python>=4.9
